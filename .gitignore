# Dependencies
node_modules/

# Build output
dist/

# Compiled server bundle (generated by `npm run clean`)
server.js

# Test coverage
coverage/

# OS artifacts
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*

# Lock files (retain package-lock.json for reproducible installs)
bun.lock

# Environment variables (keep example, ignore all actual env files)
.env*
!.env.example
.env.local
.env.*.local

# AI Studio / editor temp
assets/.aistudio/
.omo/

# IDE settings
.vscode/
.idea/
*.tsbuildinfo

# impeccable runtime state
.impeccable/config.local.json
.impeccable/hook.cache.json
.impeccable/*.png
.impeccable/live/server.json
.impeccable/live/sessions/
.impeccable/live/previews/
.impeccable/live/annotations/
.impeccable/live/cache/

# Sync files
sync.ffs_db

# Raw business data (sensitive customer & supplier data, never commit)
rawdata/
/新增資料夾/

# Generated docs — only auto-generated reports, not source docs
docs/*.mec-report.json
*.mec-report.json

# Scratch temporary artifacts (one-off ETL scripts & intermediate parse cache)
/scratch/

# Audit logs runtime checklist
docs/.audit/*.jsonl

# Chat/upload temp artifacts
/upload_*.jpg
/upload_*.png
