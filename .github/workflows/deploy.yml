name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: false

jobs:
  build-and-deploy:
    name: Quality Gate, Build & Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type check
        run: npx tsc --noEmit

      - name: MECE template validation
        run: npm run mec:check:all

      - name: Contrast ratio check
        run: node .impeccable/scripts/contrast-check.mjs src/components/

      - name: Build production bundle
        run: npm run build
        env:
          NODE_ENV: production

      - name: Deploy to gh-pages branch (Channel A)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist

      - name: Setup GitHub Pages (Channel B)
        uses: actions/configure-pages@v5
        continue-on-error: true

      - name: Upload artifact to Pages (Channel B)
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
        continue-on-error: true

      - name: Deploy to GitHub Pages (Channel B)
        id: deployment
        uses: actions/deploy-pages@v4
        continue-on-error: true
